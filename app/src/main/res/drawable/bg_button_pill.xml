<?xml version="1.0" encoding="utf-8"?>
<!--
    A pill with a lip: the face sits on a darker base that shows along the bottom edge, and a
    vertical gradient across the face turns that lip into a rounded top. Pressing drops the face
    onto the base, so the button loses its depth for as long as it is held.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_pressed="true">
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/goldDeep" />
                    <corners android:radius="100dp" />
                </shape>
            </item>
            <item android:bottom="4dp">
                <shape android:shape="rectangle">
                    <gradient
                        android:angle="270"
                        android:endColor="@color/gold"
                        android:startColor="@color/goldLit" />
                    <corners android:radius="100dp" />
                </shape>
            </item>
        </layer-list>
    </item>

    <item>
        <layer-list>
            <item android:top="5dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/goldDeep" />
                    <corners android:radius="100dp" />
                </shape>
            </item>
            <item android:bottom="5dp">
                <shape android:shape="rectangle">
                    <gradient
                        android:angle="270"
                        android:endColor="@color/gold"
                        android:startColor="@color/goldLit" />
                    <corners android:radius="100dp" />
                    <stroke
                        android:width="1dp"
                        android:color="@color/goldLit" />
                </shape>
            </item>
        </layer-list>
    </item>

</selector>
